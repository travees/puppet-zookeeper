description "ZooKeeper"
limit nofile 32768 32768

start on runlevel [2345]
stop on [!12345]

respawn
respawn limit 2 5

env ZOO_HOME=<%= @install_dir %>
umask 007

kill timeout 300

script
  exec $ZOO_HOME/bin/zkServer.sh start-foreground
end script
